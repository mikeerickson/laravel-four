// Generated by CoffeeScript 1.3.3
(function() {
  var greet, greeting, item, multi, myArr, name, obj, rand, test, _i, _len,
    __slice = [].slice;

  name = "Mike";

  greeting = "Hello " + name + "!";

  multi = " some content\nsome more here\nand one more line here \n\nMy name is " + name;

  console.log(multi);

  /* 
  	greeting
  */


  greet = function(name) {
    if (name == null) {
      name = "friend";
    }
    return "Hello " + name + "!";
  };

  console.log(greet("Mike"));

  console.log(greet("Kira"));

  /* Splat
  */


  test = function() {
    var x, y, z;
    x = arguments[0], y = arguments[1], z = 3 <= arguments.length ? __slice.call(arguments, 2) : [];
    console.log(x);
    console.log(y);
    return console.log(z);
  };

  test("one", "two", "three");

  console.log("=============");

  test("one", "two", "three", "four", "five", "six", "seven", 121, 12);

  /* anonymous funtions
  */


  (function(message) {
    return console.log("" + message + " from anonymous function");
  })("Hello");

  /* random range
  */


  rand = function(max, min) {
    if (max == null) {
      max = 10;
    }
    if (min == null) {
      min = 0;
    }
    return Math.floor(Math.random() * (max - min + 1)) + min;
  };

  console.log(rand());

  console.log(rand(100));

  console.log(rand(60, 50));

  console.log(" ");

  /* Objects
  */


  obj = {
    name: "Mike",
    address: "6372 Flint",
    city: "Huntington Beach",
    state: "CA",
    kids: ["Joelle", "Brady", "Bailey", "Trevor"]
  };

  myArr = ["a", "b", "c", "d", "e", "f", obj];

  for (_i = 0, _len = myArr.length; _i < _len; _i++) {
    item = myArr[_i];
    console.log(item);
  }

}).call(this);
